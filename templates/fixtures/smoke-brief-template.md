# Smoke brief — your standing validation test *(template)*

*One fixed, cold brief in YOUR domain. Write the pass checklist BEFORE any run. Re-run sealed (constitution + loop pasted, nothing else, no tools) on every new model and at every retro — the score trend is your drift alarm.*

## The brief (write once, never edit — paste verbatim into runs)

> Design the [screen] for "[fictional product in your domain]" — [one paragraph: who the user is, what just happened, one real constraint, one business goal]. [Deliberately leave 1–2 details thin — a good run frames or states assumptions before building.]

## What a passing run looks like (10–14 boxes, written before run #1)

**Process:** framed-or-stated-assumptions · job + story before UI · metric + guardrail named · structure chosen from known archetypes, not invented · [your §B or brand-scope handling stated].

**The screen:** [5–8 boxes specific to your brief: the one primary action; the states that matter; the copy standard; the accessibility floor checked-not-asserted; presented Problem→Insights→Solution→Impact with citations].

**Automatic fails:** raw values · invented components · fabricated owner voice · [your red lines].

## Scoring
[N] boxes. [N−2]+ = pass · borderline = weak pass, note misses · any automatic fail = fail.

<!-- Calibration log — date · model · score · grader · owner's per-box deltas -->
