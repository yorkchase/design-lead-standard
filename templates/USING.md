# USING — the owner's field guide *(template)*

*One page, task-first, every row ends in a move. Fill the rows for the tools you actually use during INSTANTIATE step 7.*

**Universal install, any tool:** [your dist repo URL] — the README's table says what to copy and where to paste. Two moves. Ask any tool *"what version are you running?"* to check freshness.

| I want to… | Do exactly this |
| --- | --- |
| Start work in [primary AI tool] | [exact move — paste tier X into slot Y; first prompt Z] |
| Start work in [second tool] | [exact move] |
| Correct mid-flow | Plain words work. Sharper: "[what's wrong] — which rule covers this? Fix it per that rule, everywhere in this project." |
| Quality-gate a screen | "Run the printed self-check table on this screen." |
| A ruling just happened | One line to `harvests/INBOX.md` (or [your phone capture note]). The retro sweeps it; you only ratify. |
| Review what's waiting on me | [The one or two places proposals land — nothing else expects a visit.] |

## Setup state
- [ ] Dist repo created + boot published
- [ ] Primary tool carries the boot ([workspace-level slot if the tool has one])
- [ ] Smoke brief written + baseline run scored
- [ ] Retro scheduled ([cadence])

*Configured [date]. `[PROPOSED]` items awaiting confirmation in use: [list].*
